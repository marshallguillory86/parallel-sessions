---
name: Bug report
about: Something in the CLI or skill misbehaves
labels: bug
---

## What happened

<!-- The actual behavior. Paste error output if any. -->

## What you expected

<!-- The expected behavior. -->

## Reproduction

```bash
# Smallest commands that trigger the bug
```

If a `plan.toml` is involved, paste the minimal version that reproduces.

## Environment

- `parallel-sessions --version` or commit SHA:
- Python version (`python3 --version`):
- OS:
- `git --version`:
- `gh --version` (if relevant):
